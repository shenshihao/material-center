{
  "name": "material-center",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"后端,前端\" -c \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
